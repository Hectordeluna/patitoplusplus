programa expresiones;
var
  int e, t;
  int matrix[10][10];

principal() {
  a = 3;
  escribe(10 + 12);
  si ((5 > 2)) entonces {
    escribe(5 + 3);
  }
  sino {
    a = 2;
    b = 3;
  }
  mientras ((10 > 20 && 12 < 30)) haz {
    escribe(10 + 30);
  }
}