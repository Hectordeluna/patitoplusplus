programa patito;
var
int i, j, k, x;
char c ,l , m;
int A[2][2];
float B[2][2];
float f;

funcion void inicia(int z)
var
int h, i;
{
  i = 5;
  mientras (i > 0) haz{
    escribe(i);
    i = i-1;
  }
}

funcion int fibo(int x)
var int y, t;
{
  si (x >= 1) entonces {
    regresa(x*fibo(x-1));
  } sino {
    regresa(1);
  }
}


principal()
{
  k = 0;
  x = 0;
  A[0][0] = 6;
  A[0][1] = 4;
  A[1][0] = 1;
  A[1][1] = -9;
  escribe("matrix", A);
  B = (A? + A) * A;
  escribe(B);
}